name: nice_wrappable_tags
description: It is a tag that can be wrapped according to the size of the device when several tags are listed.
version: 0.0.2
homepage: https://github.com/DustinPark92/nice-wrappable_tags

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
